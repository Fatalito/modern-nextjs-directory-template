name: Update Performance Baseline

# Manual workflow to update performance baseline metrics.
# 
# Run this workflow after:
# - Intentional performance improvements (lower thresholds)
# - Accepting performance trade-offs (feature worth slowdown)
# - Infrastructure upgrades (Node version, Next.js, etc.)
# - Quarterly baseline refresh

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-baseline:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.58.2-noble
    steps:
      - name: Checkout & Bootstrap
        uses: ./.github/actions/bootstrap
        with:
          install-node: 'false'

      - name: Restore/Save Next.js Build
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('package-lock.json') }}-${{ hashFiles('src/**') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('package-lock.json') }}-
            ${{ runner.os }}-nextjs-

      - name: Build application
        run: npm run build

      - name: Run Performance Benchmark
        env:
          NODE_ENV: production
          NEXT_TELEMETRY_DISABLED: '1'
          REUSE_SERVER: 'true'
        run: npx start-server-and-test 'npm start' http://localhost:3000 'npm run perf:e2e'

      - name: Update Playwright perf baseline
        run: npm run perf:baseline

      - name: Update Lighthouse baseline
        run: npm run perf:lighthouse:baseline

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@c0f553fe549906ede9cf27b5156039d195d2ece0 # v8.1.0
        with:
          commit-message: 'chore: update performance baseline from CI'
          branch: chore/update-perf-baseline
          delete-branch: true
          title: 'chore: update performance baseline'
          body: |
            ## Performance Baseline Update

            Updated baseline metrics from CI environment.

            - Runs: 5 iterations per test
            - Environment: ubuntu-latest

            **Review checklist:**
            - [ ] Playwright timing values (`perf-baseline.json`) look reasonable for CI environment
            - [ ] Lighthouse scores (`lighthouse-baseline.json`) look reasonable
            - [ ] No unexpected changes from previous baseline
