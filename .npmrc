# Suppress Node.js [DEP0040] punycode deprecation warnings.
# Root cause: @libsql/client → hrana-client → cross-fetch → node-fetch@2
# → whatwg-url@5 → tr46 → punycode (same chain brings in node-domexception).
# punycode will become a hard error in a future Node release; remove this once
# @libsql/client drops node-fetch@2 / ships a native fetch-based transport.
node-options=--disable-warning=DEP0040
